import {Container} from "@mui/material"
import styles from './Header.module.scss'

export const Header = () => {
  const isAuth = false;
  const onClickLogout = () => {};

  return (
      <div className={styles.root}>
          <Container>
        
          </Container>
      </div>
  )
}